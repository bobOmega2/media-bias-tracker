# Types Folder

Contains TypeScript type definitions used across the app.

## Examples
- `media.ts` — Type for media items (articles, videos, etc.)
- `user.ts` — Type for user objects and ratings

## Notes
- Centralized location helps maintain type safety
- Update types here when adding new media types or entities
