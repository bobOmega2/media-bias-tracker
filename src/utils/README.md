# Utils Folder

Contains pure, reusable helper functions that do not depend on external services.

## Examples
- `calculateAverage.ts` — Compute average bias score
- `formatDate.ts` — Format dates in YYYY-MM-DD
- `stringHelpers.ts` — Common string manipulations

## Notes
- Functions here should be independent and easily testable
- No database or API calls
